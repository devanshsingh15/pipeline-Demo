trigger:
- main

pool:
  name: Default   # Your self-hosted Windows agent
stages:
- stage: BuildAndTest
  jobs:
  - job: BuildAndTestJob
    steps:
      - script: |
          mvn clean install
      - script: "mvn clean test -Dlt.username=$(LT_USERNAME) -Dlt.key=$(LT_ACCESS_KEY)"
        displayName: "Command Line Script"
      - task: LambdatestResults@1



