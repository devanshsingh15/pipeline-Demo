trigger:
- main

pool:
   Default
stages:
- stage: BuildAndTest
  jobs:
  - job: BuildAndTestJob
    steps:
      - script: "mvn clean test"
        displayName: "Command Line Script"
      - task: 
          LambdatestResults@1